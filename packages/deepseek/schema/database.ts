// DeepSeek's 4 operations (chat completions, Anthropic-compatible messages,
// balance, models) are all stateless request/response calls — there is no
// resource worth persisting locally, so this plugin defines no database entities.
